diff --git a/pyproject.toml b/pyproject.toml
index fcc0aff60..e779bc0d2 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools~=65.5", "pip~=22.3", "wheel"]
+requires = ["setuptools", "pip", "wheel"]
 build-backend = "setuptools.build_meta"

 [project]